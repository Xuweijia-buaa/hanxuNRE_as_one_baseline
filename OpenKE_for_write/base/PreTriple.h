#ifndef PRETRIPLE_H
#define PRETRIPLE_H
#include "Setting.h"
#include <vector>
struct Pre_Triple {
	INT h, r;
	std::vector<INT> vec;
	//vector<INT> vec;  // vec.push_back(i)
	// int exist = std::count(vec.begin(), vec.end(), value);
};

#endif
